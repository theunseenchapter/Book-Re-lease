<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="profile.css">
</head>
<body>
    <header>
        <h1>Profile</h1>
        <nav>
            <ul class="nav-menu">
                <li><a href="browse">Browse Books</a></li>
                <li><a href="index">Post Your Book</a></li>
                <li><a href="profile">Profile</a></li>
                <li><a href="login" id="logout">Logout</a></li>
            </ul>
        </nav>
    </header>
    
    <div class="container">
        <!-- <%= user%> -->
        <!-- <section class="profile-info">
            <h2>User Information</h2>
            <div class="profile-item">
                <h3>Name: <span><%= user.name %></span></h3>
            </div>
            <div class="profile-item">
                <h3>Class: <span><%= user.class %></span></h3> 
            </div>
            <div class="profile-item">
                <h3>College Year: <span><%= user.college_year %></span></h3>
            </div>
            <div class="profile-item">
                <h3>Roll No: <span><%= user.rollNo %></span></h3> 
            </div>
            <div class="profile-item">
                <h3>Email: <span><%= user.email %></span></h3>
            </div>
            <div class="profile-item">
                <h3>Phone No: <span><%= user.phone_no %></span></h3>
            </div>
            <div class="profile-item">
                <h3>Gender: <span><%= user.gender %></span></h3>
            </div>
            <div class="profile-item">
                <h3>Fees Status: <span><%= user.fees_status %></span></h3> 
            </div>
            <div class="profile-item">
                <h3>Academic Status: <span><%= user.academic_status %></span></h3>
            </div>
            <div class="profile-item">
                <h3>Address: <span><%= user.address %></span></h3> 
            </div>
            <% if (user.aadhar_card_no) { %> 
            <div class="profile-item">
                <h3>Aadhar Card No: <span><%= user.aadhar_card_no %></span></h3> 
            </div>
            <% } %>
        </section> -->
    </div>

    <script>
        // Logout functionality
        document.getElementById('logout').addEventListener('click', function(e) {
            e.preventDefault();
            fetch('/logout', {
                credentials: 'include'
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    window.location.href = 'login'; // Redirect to login page
                }
            })
            .catch(error => console.error('Error:', error));
        });
    </script>
</body>
</html>
